# CakeSplitter v0.6.0 Local Release Audit

This audit covers the source release at the v0.6.0 finalization checkpoint.
Public GitHub publication is intentionally deferred until v0.8.0.

## Repository state

- Required branch: `feat/v0.6-cli-developer-workflows`.
- RC parent: `31c95294a4921b32671ee6d51535750984fdd0a6`.
- Baseline tag `v0.5.0` remains unchanged.
- No remote is configured and no push is authorized.
- Existing v0.2.1, v0.3.0, v0.4.0, and v0.5.0 tags remain unchanged.
- The pre-existing local DOCX remains excluded through `.git/info/exclude`.

## Version and contract audit

Application, workspace packages, Cargo metadata, Tauri metadata, UI display
fallbacks, and test fixtures report `0.6.0`. CLI schema and Batch Job schema
remain `1`; Cake Package format remains `1.0`. No development-version marker
remains in the tracked source release.

## Artifact audit

The unsigned executable and NSIS installer are preserved outside Git in the
local `CakeSplitter-v0.6.0-artifacts` directory. Both entries in its
`SHA256SUMS.txt` manifest were recomputed successfully. The executable reports
file and product version `0.6.0` and remained alive during a five-second launch
smoke check before the test process was stopped. The executable matched across
two `/Brepro` release builds; NSIS embeds build-time data and is preserved as
the single canonical installer build.

## Repository hygiene

The release commit contains no `.agents`, `.codex`, `node_modules`, Rust
`target`, Web or Desktop `dist`, Playwright results, test-result folders,
temporary scan workspaces, browser profiles, secrets, machine-specific paths,
or generated binaries. `git diff --check`, `git fsck --no-dangling`, the
focused security review, and the complete validation matrix passed.
