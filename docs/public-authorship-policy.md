# Public Authorship Policy

## Authorized identity

- `PUBLIC_AUTHOR_NAME = Yu-En Huang`
- `PUBLIC_COMMIT_EMAIL = enmity.coyotes.3o@icloud.com`
- `HISTORY_POLICY = preserve-complete-history`

The v0.7 history audit verified that every reachable author, committer, and
annotated-tagger record already used this authorized public identity. The
selected Option B identity transform was therefore idempotent: no commit hash,
tag object, tag target, message, timestamp, or tree changed.

Future public commits and annotated tags use the same repository-local
identity. Private preservation bundles and the identity mapping remain outside
the public repository.

History must not be rewritten solely to improve presentation. A later rewrite
requires a validated sensitive-history issue, an explicit review, replacement
preservation evidence, and complete revalidation.
