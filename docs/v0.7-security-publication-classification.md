# v0.7 Security Publication Classification

Tracked historical security material was reviewed for public-history safety.

- Public unchanged: sanitized design/security limits, v0.6 review material,
  contract-alignment evidence, and this classification/index.
- Public summary only: v0.2, v0.3, v0.4, and v0.5 detailed reports; the
  sanitized cross-version summary is the public substitute.
- Private outside a future public history: sealed scan evidence, raw PoCs,
  internal scan identifiers, backup paths, and detailed remediation reports
  that contain operational evidence.
- No raw scan workspace, browser profile, private DOCX, backup, or PoC binary is
  copied into the candidate source tree.

The current history contains the classified reports, so a v0.8 publication
must either sanitize them throughout history, create a separately authorized
public history root, or obtain explicit acceptance of each remaining public
report. The classification does not claim that a tracked private report is
already safe for publication.
