# CakeSplitter v0.5.0 Test Report

This report records the v0.5 local-release validation matrix. Commands are
run from a clean source checkout with lockfiles. Application version `0.5.0`
and Cake Package format `1.0` are separate.

## Focused remediation checks

- Desktop runtime: 40 unit tests, 7 engine integration tests, 6 operational
  reliability tests, and 1 queue-endurance test passed after the four Medium
  fixes.
- Tauri runtime: `cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml`
  passed.
- Receipt and diagnostic directory replacement harnesses failed closed with
  no output in the replacement destination.
- Redaction production tests and independent D-drive/UNC harness passed.
- Failed-history cleanup harness removed both the valid record and its owned
  partial output.

## Complete matrix

Final clean-run results:

- `cargo fmt --all -- --check` — passed.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` —
  passed with zero warnings.
- `cargo test --workspace` — 122 tests passed, 1 explicit 1 GiB profile
  ignored, 0 failed.
- `cargo build --workspace --release` — passed.
- `npm run lint` and `npm run typecheck` — passed.
- `npm test` — 98 tests passed across 10 files.
- `npm run test:compatibility` — Rust-to-Web, Web-to-Rust, and malicious
  manifest checks passed.
- `npm run build` and `npm run build:desktop` — passed.
- `npm run test:e2e` — 12 production Edge tests passed, including offline,
  privacy, OPFS, accessibility, and Clear All flows.
- `npm audit` and `npm audit --omit=dev` — zero vulnerabilities.
- `cargo audit` — zero vulnerability advisories; 17 accepted upstream
  unmaintained/unsound warnings are documented in the security report.

The packaged executable launch check remained running for five seconds and was
closed cleanly. The final private artifacts are outside Git source:

| Artifact | Size | SHA-256 |
| --- | ---: | --- |
| `cakesplitter-desktop-0.5.0.exe` | 13,897,728 bytes | `C46CF801FB99B9448F62ED415EAF0905717248CAD95AFFBC26559FDFFD746351` |
| `CakeSplitter Desktop_0.5.0_x64-setup.exe` | 3,478,713 bytes | `4FB3CF394D7113AA5884D08E6B6F91BCDBBFF5C3F0BE47A0A76A4689CF816BFF` |

Interactive installer lifecycle and desktop queue/reorder/pause/resume UI
automation were not executed in this session because no callable Windows UI
automation surface was available. This is an explicit validation limitation,
not a claim of success.

## Environment notes

Native Cargo builds use the repository lockfile and a dedicated external target
directory to keep generated output out of the source release. No node_modules,
Rust target, Web dist, Playwright report, installer, browser profile, or scan
workspace is a release source artifact.
